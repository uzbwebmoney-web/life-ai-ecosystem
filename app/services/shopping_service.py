from __future__ import annotations

from app.core.i18n import t

SHOPPING_SUBMODULE_AI: dict[str, str] = {
    "compare": (
        "Сравнивай товары по ключевым параметрам: цена, качество, функции, плюсы и минусы. "
        "Структурируй ответ в виде таблицы или списка."
    ),
    "deals": (
        "Помогай искать выгодные предложения: на что смотреть, сезонные скидки, "
        "как отличить реальную скидку от маркетинговой. Без ссылок на конкретные магазины."
    ),
    "specs": (
        "Объясняй характеристики товаров простым языком: что означает каждый параметр, "
        "на что обратить внимание, типичные подводные камни."
    ),
    "advice": (
        "Давай советы по выбору товаров под бюджет и задачи пользователя. "
        "Задавай уточняющие вопросы, предлагай несколько вариантов."
    ),
}


def shopping_submodule_description(sub_id: str, lang: str) -> str:
    key = f"shop_sub_{sub_id}"
    text = t(lang, key)
    return text if text != key else t(lang, "shop_ai_hint")
